import { FC } from "react";
import { Link } from "react-router-dom";

const ProfileLink: FC = () => {
  return (
    <Link to="/bookmarked">
      <img src="" alt="avatar" />
    </Link>
  );
};

export default ProfileLink;
